# Text.NewGuid
Returns a new, random globally unique identifier (GUID).
***
function () as text
***
# Descrition 
Returns a new, random globally unique identifier (GUID).
# Category 
Text.Conversions from and to text
